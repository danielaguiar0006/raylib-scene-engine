#pragma once

#include "scenes/Scene.h"

class Renderer {
public:
    Renderer() {}
    ~Renderer() {}

    void Init() {
        // Initialize the renderer
    }

    void Render(Scene* scene) {
        // Render the scene
    }

    void Cleanup() {
        // Cleanup the renderer
    }
};
